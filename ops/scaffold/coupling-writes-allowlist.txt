# Escrituras en tablas de otro esquema sancionadas: "<servicio> <esquema>", con su motivo.
# El dueno del esquema expone la operacion (API interna o evento) y quien la necesita la
# pide; una entrada aqui es una excepcion que se justifica y se retira.
