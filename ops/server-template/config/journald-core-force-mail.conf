# Tope del journal de systemd. Sin limite crece sin freno en un disco que comparte con
# todos los contenedores y con Postgres; los logs de los servicios viven en
# Docker (json-file con rotacion en daemon.json), asi que aqui solo queda el sistema.
[Journal]
SystemMaxUse=300M
