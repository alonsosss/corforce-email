# GENERADO por ops/ecr/gen-compose-images.sh - no editar a mano.
# Uso: docker compose -f docker-compose.yml -f docker-compose.images.yml pull <svc> && up -d
services:
  gateway:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/gateway:${DEPLOY_TAG:-latest}
  identity:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/identity:${DEPLOY_TAG:-latest}
  access-control:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/access-control:${DEPLOY_TAG:-latest}
  organization:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/organization:${DEPLOY_TAG:-latest}
  audit:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/audit:${DEPLOY_TAG:-latest}
  scheduler:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/scheduler:${DEPLOY_TAG:-latest}
  observability:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/observability:${DEPLOY_TAG:-latest}
  mail-directory:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/mail-directory:${DEPLOY_TAG:-latest}
  mail-auth:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/mail-auth:${DEPLOY_TAG:-latest}
  mail-security:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/mail-security:${DEPLOY_TAG:-latest}
  webmail:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/webmail:${DEPLOY_TAG:-latest}
  domain-service:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/domain-service:${DEPLOY_TAG:-latest}
  mail-migration:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/mail-migration:${DEPLOY_TAG:-latest}
  mail-dav:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/mail-dav:${DEPLOY_TAG:-latest}
  transactional:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/transactional:${DEPLOY_TAG:-latest}
  smtp-relay:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/smtp-relay:${DEPLOY_TAG:-latest}
  suppression:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/suppression:${DEPLOY_TAG:-latest}
  templates:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/templates:${DEPLOY_TAG:-latest}
  contacts:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/contacts:${DEPLOY_TAG:-latest}
  billing:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/billing:${DEPLOY_TAG:-latest}
  reputation:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/reputation:${DEPLOY_TAG:-latest}
  campaigns:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/campaigns:${DEPLOY_TAG:-latest}
  automations:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/automations:${DEPLOY_TAG:-latest}
  analytics:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/analytics:${DEPLOY_TAG:-latest}
  mail-files:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/mail-files:${DEPLOY_TAG:-latest}
  web:
    image: ${ECR_REGISTRY:?definir en .env}/core-force-mail/web:${DEPLOY_TAG:-latest}
